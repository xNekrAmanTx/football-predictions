// import firebase from 'firebase/app';
// import 'firebase/database';

// export default (setter, leagueId, roundId, fixtureId) => firebase.database().ref(`/fixturesPerLeaguePerRound/${leagueId}/${roundId}/${fixtureId}`)
// .on('value', setter);